import React from 'react';
import Stack from "@mui/material/Stack";
import {PaperContainer} from "@/components/common-styled";
import PaperHeader from "@/components/dashboard/surfaces/PaperHeader";

const TransactionsTimeline = () => {
  return (
    <PaperContainer>
      <PaperHeader title='Timeline de entradas e saídas'/>
      <Stack>
        dadas
      </Stack>
    </PaperContainer>
  );
};

export default TransactionsTimeline;