export enum AuthType {
    LOGIN,
    REGISTER,
    FORGOT_PASSWORD,
  }

export const TransactionTypeEnum = {
    INCOME: "Receita",
    SPENDINGS: "Despesa",
};

export enum EndDateEnum {
    TODAY,
    END_OF_MONTH,
}